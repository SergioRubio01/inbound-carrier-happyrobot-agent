/**
 * @file: postcss.config.mjs
 * @description: PostCSS configuration file, primarily for integrating Tailwind CSS.
 */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;
